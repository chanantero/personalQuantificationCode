function Txml = formattedTable2XMLtable(T, extendedScheme, XSDfile)
% T is a formatted table. This means that it is a table formed by the
% information of an XML table once the attributes described by the XSD
% schema are extended.
% This function reverse the process and converts the extended attributes
% (each one is a column) into a compact version (the one of an XML
% table).


end