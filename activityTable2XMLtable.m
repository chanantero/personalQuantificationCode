function Txml = activityTable2XMLtable(Tact, attributeNames)
% Tact is a table. Each row is an activity. Each column is an attribute of
% the activity.
% Tattrib is a table with 4 variables/columns: 
% [attributeName, type, use, default, custom, enumeration, external].

% Transform all columns of Tact to string format according to the type.


end

